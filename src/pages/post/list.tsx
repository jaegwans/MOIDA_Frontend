import { useRouter } from 'next/router';
import Link from 'next/link';

const List = () => {
    const router = useRouter();

    return (
        <div>
            <h1>post list</h1>
        </div>
    );
};

export default List;
